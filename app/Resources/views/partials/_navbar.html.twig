<nav class="navbar navbar-expand-md navbar-light pb-2">
  <a class="navbar-brand py-0" href="{{ path('homepage') }}">
    <img src="{{ asset('symfo_eval_faqoclock_logo/symfo_eval_faqoclock_logo-3_transparent.png') }}" width="50" height="50" alt="">
  </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <!-- <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="{{ path('homepage') }}">Acceuil</a>
      </li>
    </ul> -->
    <ul class="navbar-nav ml-auto">
      <li class="nav-item my-auto">
        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
          Welcome <strong><a href="{{ path('user_profile') }}">{{ app.user.username }}</a></strong>
        {% else %}
          Welcome guest
        {% endif %}
      </li>
        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
          <li class="nav-item ml-2">
            <a class="nav-link btn btn-warning text-dark" href="{{ path('logout') }}">Logout</a>
          </li>
        {% else %}
          <li class="nav-item ml-2">
            <a class="nav-link btn btn-success text-white" href="{{ path('login') }}">Login</a>
          </li>
          <li class="nav-item ml-2">
            <a class="nav-link btn btn-secondary text-white" href="{{ path('signin') }}">Signin</a>
          </li>
        {% endif %}
    </ul>
  </div>
</nav>
