{% block extends %}
{{ "{% extends 'base.html.twig' %}" }}
{% endblock extends %}

{{ "{% block title %}" }}{{ entity_singularized|capitalize }} new{% if 'admin' in route_name_prefix %} [admin]{% endif %}{{ "{% endblock %}" }}

{% block body %}
{{ "{% block body %}" }}
    <h1 class="font-weight-bold text-center bg-dark text-white rounded mb-3 py-2">{{ entity_singularized|capitalize }} creation</h1>

    {{ '{{ form_start(form) }}' }}
        {{ '{{ form_widget(form) }}' }}
        <input class="btn btn-success" type="submit" value="Create">
    {{ '{{ form_end(form) }}' }}

{% set hide_edit, hide_delete = true, true %}
{{ include('crud/views/others/record_actions.html.twig.twig') -}}
{{ "{% endblock %}" }}
{% endblock body %}
